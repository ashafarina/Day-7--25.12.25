public class GPACalculator {
    public static void main(String[] args) {

        double[] grades = {8.5, 7.2, 9.0};
        double sum = 0;

        for (double g : grades)
            sum += g;

        System.out.println("GPA: " + (sum / grades.length));
    }
}
